services:
  postgres:
    networks:
      - data
      - devhost
    ports:
      - "127.0.0.1:${POSTGRES_DEV_PORT:-5433}:5432"
  migrate:
    networks:
      - data
      - devhost
  db-permissions:
    networks:
      - data
      - devhost

networks:
  devhost:
